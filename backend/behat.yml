default:
  suites:
    api:
      contexts:
      - ApiContext
    frontend:
      contexts:
      - FrontendContext
  extensions:
    Behat\MinkExtension:
      base_url:  'http://frontend'
      sessions:
        default:
          goutte: ~