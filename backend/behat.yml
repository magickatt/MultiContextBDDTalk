default:
  suites:
    api:
      contexts:
      - ApiContext
    frontend:
      contexts:
      - FrontendContext
  extensions:
    Behat\MinkExtension:
      base_url:  'http://localhost:9999'
      sessions:
        default:
          goutte: ~